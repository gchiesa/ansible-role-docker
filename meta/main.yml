---
galaxy_info:
  author: Giuseppe Chiesa
  description: Docker / Docker Compose support for Enterprise Linux
  company:
  license: BSD
  min_ansible_version: 2.0
  platforms:
    - name: EL
      versions:
        - 7
  galaxy_tags: [docker, dockercompose]
dependencies: 
    - { role: 'gchiesa.glusterfs', extra_modules: ['docker'] }

